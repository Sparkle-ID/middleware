apiVersion: apps/v1
kind: Deployment
metadata:
  name: did-service-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: did-service-mongo
  template:
    metadata:
      labels:
        app: did-service-mongo
    spec:
      containers:
        - name: did-service-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: did-service-mongo-srv
spec:
  selector:
    app: did-service-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
